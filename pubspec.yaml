name: flutter_telpo
description: A new flutter plugin project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  
flutter:
  plugin:
    platforms:
      android:
        package: com.fluttertelpo.flutter_telpo
        pluginClass: FlutterTelpoPlugin

assets:
  - assets/flutter_assets/packages/flutter_telpo/assets/images/icon.png
  #

 